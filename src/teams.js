// Цвета команд по constructorId Jolpica (ID собраны из API за 2018–2026).
// Оттенки взяты от ливрей, но прогнаны через проверку на тёмном фоне: у всех
// контраст >= 3:1, соседние пары различимы и при дальтонизме. Три правки
// осознанные — ливреи Ф1 не были нарисованы для того, чтобы их различали
// в таблице:
//   audi     — титан ливреи на тёмном фоне читается как серый (а серым у нас
//              помечены выключенные круги), поэтому взят розовато-титановый;
//   rb       — фирменный #6692FF неотличим от синего Williams (ΔE 13.3),
//              уведён в индиго;
//   cadillac — ливрея чёрно-белая, то есть не кодирует принадлежность вообще;
//              взят золотой из эмблемы.
// ponytail: цвета новых Audi и Cadillac — приближение, официальных hex нет.
// Не понравится — правится здесь одной строкой, больше нигде не используется.
const COLORS = {
  mclaren: '#FF8000',
  ferrari: '#E8002D',
  red_bull: '#3671C6',
  mercedes: '#27F4D2',
  aston_martin: '#229971',
  alpine: '#FF87BC',
  williams: '#64C4FF',
  rb: '#8B7FE8',
  audi: '#C98A9B',
  cadillac: '#C9A227',
  haas: '#E9EEF4',
  // Команды прошлых сезонов — в одном сезоне с Audi и Cadillac не встречаются.
  sauber: '#52E252',
  alfa: '#D42A45',
  alphatauri: '#4C7FBF',
  racing_point: '#F596C8',
  renault: '#FFD800',
  force_india: '#FF5FA2',
  toro_rosso: '#469BFF',
};

const FALLBACK = '#9AA4B8';

export const teamColor = (constructorId) => COLORS[constructorId] || FALLBACK;

// Чёрный или белый текст поверх цвета команды — по воспринимаемой яркости.
export function onColor(hex) {
  const n = parseInt(hex.slice(1), 16);
  const [r, g, b] = [(n >> 16) & 255, (n >> 8) & 255, n & 255].map((c) => {
    const s = c / 255;
    return s <= 0.03928 ? s / 12.92 : ((s + 0.055) / 1.055) ** 2.4;
  });
  return 0.2126 * r + 0.7152 * g + 0.0722 * b > 0.36 ? '#10131a' : '#ffffff';
}
